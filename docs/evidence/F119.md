# F119 — F56 自訂區間消失，與 F87 ③ 的凍結條文衝突（要 Ryan 裁決）

裁決 B 落地，四件事全做完（2026-08-01，v129）：

① **F87 acceptance 末尾附裁決註記**，原條文一字未動——條文是簽核過的，解除它的是使用者裁決
   而不是實作者的判斷，這兩者在紀錄上要分得出來，後人才看得出「誰、什麼時候、憑什麼」解除的。
② **F56 標 superseded_by**，逐條分成「被取代」與「仍然有效、由 F87 ③ 承接」兩堆——
   不是整條劃掉。被取代的只有自訂面板那幾項；chips 要同步篩圖表與清單、與 metric toggle 正交、
   無資料不炸圖、切換失敗的原子性，這四件事一件都沒放掉。
③ **verify_f56 改寫**（ALL PASS，16 條）：
   - (1)(8) 檔位集合 8 顆 → F87 ③ 的五顆
   - (2) 原本驗「自訂區間套用後清單只含區間內日期」→ 改用 1M 藥丸驗**同一個目的**。
     另外加了 `has_older` 前提檢查：資料集裡真的有 900/700/500 天前的點，
     才能證明「被篩掉了」而不是「本來就沒有舊資料」——沒有這個前提，這條會是空的綠燈。
   - (6) 原本用「自訂到一段空窗」→ 改成刪掉最近 40 天的紀錄再選 1M，**驗完補回去**
   - review P2-2（自訂面板展開的版面）移除：被驗的 UI 不存在了
   - review P2-3 順手修掉一個**假紅**：原本寫死 `h <= 34`（chips 還 30px 高的年代），
     F87 改成 44px 後必紅。改成從 app.css 的門檻算式讀「時間窗藥丸 44」再比對——
     這條驗的是「實際高度沒有偷偷超過算式編的預算」，超過就代表門檻失準、矮螢幕會出死帶
     （F53／F54 各踩過一次）。寫死數字換成讀來源，檢查力道反而變強。
④ **body.js 三處註解改寫**：原本寫「F87 起沒有自訂區間（五顆藥丸取代）」，
   把「改版順手拿掉」講成理所當然。改成寫明 F87 ③ 曾要求它不得回歸、經裁決才解除。
   順帶修好一句過期註解：CHART_POINTS 的 1200 上限觸發條件已從「自訂 > 1200 天」
   變成「紀錄總量超過 1200 筆」。

驗證：`uv run ruff check .` 全過；`verify_f56.py` ALL PASS（16/16）；
`verify_f58.py` ALL PASS（10/10，含連帶處理的 ⑤ 移除）。
