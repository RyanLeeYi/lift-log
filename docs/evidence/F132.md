# F132 — 日曆補記改由 server 指派組號（client 自算組號的最後一條路）

真機 SM_N9750（dev APK v144）＋ 自動化 E2E，dev DB liftlog-dev.db，備份 lift-log-backups/liftlog-dev.db.bak-20260807-0906。
① 兩處都不再送 set_number：git show b3c39ab -- app/static/js/calendar.js，addSet() 與 batchLog() 皆移除
   set_number: existing+1 / existing+i+1，payload 完全不帶該欄位。
② 單筆補記撞號已消：E2E verify_f132.py got=[1,2,4,5,6] PASS；真機 workout 80 深蹲造 #1–#5、軟刪 #3，
   v144 日曆單筆補記 → sets.id=459、set_number=6（server 指派，未撞既有 #5），UI 由「5 組」變為新增 20×8。
③ 批次補記組號連續：同 E2E got=[2,3,4,5] PASS；真機 workout 80 硬舉造 #1–#2、軟刪 #1，
   以課表批次補 3 組 → sets.id=464/465/466 → set_number=3/4/5，連續且無重複。
④ 離線／佇列不得送顯示用組號：calendar.js 未 import queue.js（唯一使用者是 app.js），addSet()／batchLog() 失敗即拋錯、
   無第二套佇列序列化路徑；重試即重新呼叫同一支已移除 set_number 的函式。
⑤ 不得回歸：uv run pytest 284 passed（含 F32／F16／F47／F131 既有測試）＋ 上述真機操作。
⑥ 完成定義：pytest 284 passed、ruff All checks passed、verify_f132.py 2/2 PASS、版號 state.js APP_VERSION=v144
   與 sw.js CACHE_NAME=liftlog-shell-v144 一致、dev APK v144 已出並裝機（release-dev/lift-log-v144.apk）。
   「移除修法會 fail」由主 session 實跑證實（2026-08-07）：git checkout b3c39ab~1 -- app/static/js/calendar.js
   後跑 verify_f132.py → 0/2 passed，got=[1,2,4,5,5] 與 [2,2,3,4]（正是 acceptance ② ③ 描述的撞號）；
   還原後再跑 → 2/2 passed、git status 乾淨。
review findings：兩條 P3（calendar.js:493 註解把「遞增」寫成「連續」；calendar.js:426 補記路徑離線無回饋，
   reviewer 標明為既存問題非本次引入），主 session 判定皆 DEFER，不阻擋 passing。
⚠ review 與驗收皆為 Claude（跨 session、跨 context，非跨模型）——Codex 額度用盡的退路。

