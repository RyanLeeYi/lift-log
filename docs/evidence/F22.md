# F22 — 課表加動作視窗：部位篩選（同 logger picker）

acceptance-verifier 8/8 全 pass（Playwright + httpx 實測）：加動作視窗有部位 chips（6 部位）；點部位縮清單（total 35→9、chip .on 唯一）；再點同部位取消回全部；部位＋搜尋可並用且搜尋框不失焦；篩選下可選動作按確定加入、存檔課表含該動作；開窗重置部位篩選；去重仍生效。codex-review 1 P2 已修（部位 chip 改就地更新不整頁重繪，避免與進行中搜尋 callback 競態）。sw v14。commit 串含 F22 impl + P2 fix。uv run pytest 152、ruff clean。E2E verify_f22.py。
