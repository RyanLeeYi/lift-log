# F96 — 編輯課表拿掉第一張動作卡的高亮

（2026-07-31 v107）拿掉 `.tpl-item.hi`：templates.js 不再對 index===0 加 hi，app.css 移除 `.tpl-item.hi` 與 `.tpl-item.hi .round-btn` 兩條規則（③：沒有加任何替代高亮，高亮要等 F97 綁到「正在拖的那張」才回來）。
⑤ verify_f88 的 ② 斷言改成驗底色一致（first_bg == other_bg），不是刪掉——一致性同樣需要有人守著，下一個誤加高亮的實作要在那裡掛掉。
⑥ 完成定義：ruff All checks passed；pytest 274 passed；tests/e2e/verify_f88.py 31/31（含 ⑦ F21 清單捲動門檻與捲動位置保留、⑥ F51 底部三顆鈕、⑧ 觸控目標 44px，F21／F51／F52 均不回歸）。
④ F88 ② 的「第一張（或選中的）用 --card-hi」已標 superseded_by 本條，F88 其餘條文不變。
reviewed_by / verified_by：見下方 session 註記——這批的 review 與驗收在 Codex 額度恢復前都是同模型 fresh context，獨立性較弱。
