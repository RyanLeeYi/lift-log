# F20 — 開練頁 done-list 新→舊排序＋固定高度可捲動

acceptance-verifier 7/7 全 pass（Playwright 實測）：done-list 新→舊排序（#3 在最上、#1 在最下）；組數>2 加 scrollable class 且 scrollHeight>clientHeight（max-height 88px overflow-y auto）、主按鈕仍在畫面；≤2 組不捲動；編輯某列時解除 scrollable（編輯表單完整可見）；跨動作草稿清除（換動作後 scrollable 恢復）。codex-review 1 P2 已修（finish/endWorkout 清 editDraft，避免殘留使 scrollable 永久失效）。sw v12。commit 1fdcf0d + P2 fix。uv run pytest 152、ruff clean。E2E verify_f20.py。
