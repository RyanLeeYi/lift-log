# F27 — 課表編輯未儲存離開提醒（beforeunload＋返回確認）

2026-07-19 完成。①beforeunload（window 級，重整/關閉）：hasUnsavedTemplate() 比對草稿快照 vs 進場基準，未儲存時 preventDefault。②app 內返回確認：點「← 課表列表」若未儲存 → 自訂 .confirm-modal（捨棄並離開／繼續編輯），非瀏覽器 confirm。codex-review 完成、2 P2 已修：P2①休息欄位加 oninput 即時寫草稿（Chrome 先 beforeunload 後 change，否則打字未失焦重整會漏警告）；P2②controllerchange 設 refreshing 後加 setTimeout 復原，避免 beforeunload 取消重載後 latch 卡住讓 F14 自動更新永久失效。sw.js v25、APP_VERSION v25。E2E：verify_f27.py（beforeunload＋休息打字回歸）、verify_f27b.py（返回確認 4/4）皆 ALL PASS；F25/F26 回歸 ALL PASS、pytest 161、ruff clean。註：行動瀏覽器（尤其 iOS Safari）beforeunload 支援有限；app 內返回確認不受此限、各平台皆有效。
