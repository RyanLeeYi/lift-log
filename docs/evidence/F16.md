# F16 — 訓練組 編輯＋刪除

acceptance-verifier R1–R14 全 pass（含 R9 複驗）：後端 PATCH /api/sets/{id} 原位更新、set_number 不變/404 未知+已軟刪/範圍 400（TestUpdateSet 7 測）；logger done-list 與日曆明細每組可用 stepper +/- 編輯（驗收者實測編輯態 0 個 input、2 個 stepper，PATCH server 生效、set_number 不變）＋兩段式刪除（軟刪、明細少一筆）；離線 queued 組刪除移出佇列、同步後回填 server id。codex-review 3 條已修：P1（flushQueue 回填 id 至 doneSets）、P2（刪除更新 setCounts）、R9（編輯改用 steppers 非 number input）。CLAUDE.md sets 規則已更新。commits 4cad81d/eceb448/6f8babe/6e39f97/5bb518d。E2E verify_f16_logger/calendar/p1（scratchpad）。uv run pytest 146 passed、ruff clean。
