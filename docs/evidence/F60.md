# F60 — 用課表批次新增：每列摺疊成摘要，點開才微調

2026-07-27：上一場（7/26）程式碼已落地但一次都沒驗過，本場只做驗證未重寫。實作者 E2E `verify_f60_own.py` **9/9 PASS**（4 列收合摘要含自體重「負重 0kg」、點標頭展開／收合可多列並存、點勾選框只切勾選態不動展開態、stepper 調整後摘要就地更新為 `22.5kg × 9 × 3 組` 且清單 scrollTop 472→472 不變、390×844 與 360×640 皆 4 列全可見不需捲動、「取消」退回課表清單、重開全收合、全部記錄只寫勾選列）；pytest 195、ruff clean、F49 補記 modal 回歸 17/17。
**腳本修正（非實作缺陷）**：E2E 最後一條用了不存在的 `/api/workouts/{id}/sets`，改為 `GET /api/workouts/{id}` 取 `sets` 欄位。
**跨模型 review（`/codex-review`，Codex 額度已恢復）無 findings**——Codex 自行重跑 F60 E2E、F49 回歸與 195 項 pytest 後確認無 correctness／regression 問題。
**跨模型驗收（`/codex-verify`）①–⑦ 全 pass**，且 `git status` 零改動（驗收者未越權）。它額外驗到實作者腳本沒涵蓋的兩件事：①有歷史紀錄的動作預設值確實取最重組（`42.5kg × 11`），非只驗無歷史的 20×8 ②**部分失敗重試路徑**——刻意讓第 2 組回 500 後重試，workout POST 總數仍為 1、UUID 序列 `[A,B,A,B,C]` 證明沿用而非重建（F47 那條 P1 的回歸防線仍在）。
上場列的三個風險全數被證偽：③ 的 `closest('.batch-pick')` 擋冒泡後勾選態真的變了（true→false、class 加 `off`、展開態不動）；`paint()` 每次 replaceChildren 重建整列對連點與 caret 同步無害；⑤ 收合列實測高 24px（標頭）、清單 190px，遠低於 360×640 的預算。
