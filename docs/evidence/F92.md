# F92 — 空的 workout 不該看起來像練過

2026-07-30：ruff clean、pytest 267 passed（新增 TestDeleteWorkout 4 條）、tests/e2e/verify_f92.py 15/15；回歸 verify_f90 30/30、verify_f91 20/20、verify_f85 92/92。commit a3e859d..d88a728。review：/codex-review 跨模型，2 條 findings 全部成立、全部修掉——P1 清理前的 DB 備份被 git add -A 掃進 commit（含 push_subscriptions 憑證與體重體脂；未推送過，已從 commit 移除、備份移出 repo、blob 已 gc，.gitignore 補 *.db.bak*）；P2 離線結束空 workout 只記 id 不記動作，回線上會補成標記結束而永遠不刪（佇列改帶 mode）。驗收：2026-07-30 codex-verify 跨模型，①–⑪ 全部 pass，事後 git status 乾淨。驗收者另驗到我沒測的情境：同日「空場課表」與「有效課表」兩場並存時，標題只取有有效組的那場。
