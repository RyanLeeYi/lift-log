# F136 折線圖浮動資訊要能用鍵盤與螢幕閱讀器打開

## 狀態
- 實作：2026-08-19 commit `282f1b7`（executor worker，worktree）——`.line-pt` role=button＋tabindex=0，Enter/Space 與滑鼠共用 `selectChartPoint()`；`.line-tip` role=status；rerender 後 `focusChartPoint()` 找回焦點；`:focus-visible` 沿用 --accent
- 升號：`15adc46`（v155）；APK `lift-log-v155-F158-F136.apk`（與 F158 同顆）
- 獨立驗收：2026-08-19 `acceptance-verifier`（fresh context，同模型），①–⑤ 全 pass；真機 TalkBack 依規格標 **unverified**

## 完成定義輸出（驗收者實跑）
| 指令 | 結果 |
|---|---|
| `uv run ruff check .` | All checks passed |
| `uv run pytest tests/test_exercises.py` | 10 passed |
| `verify_f136.py` | 25/25 |
| `verify_f134.py`／`verify_f86.py`／`verify_f113.py`（回歸） | 60/60／38/38／15/15 |
| 驗收者自寫獨立 Playwright 走查 | 8/8 |
| 移除修法會 fail | 驗收者以一次性 clone 還原 `282f1b7^` 內容實測：role/tabindex 為 None、Enter/Space FAIL、`.line-tip` TimeoutError |

## 備註
- 派工單要驗收者用 `git stash` 驗「移除修法會 fail」，與它自身的 `verifier-bash-guard`（擋 stash）衝突；驗收者正確地沒有繞過，改用 clone 副本等效驗證。**之後派驗收不要要求 stash**，寫「用你能用的唯讀方式（clone／`git show` 到暫存目錄）」
- Space 一律 preventDefault 防捲動（沿用 dom.js 慣例）；Enter/Space 開啟後也補焦點（rerender 整棵重建）
