# F134 — 動作表現頁改用可點選的折線圖（點資料點顯示日期／組數／最佳組）

實作於 worktree 分支後併入 main（63962a4）；⑧ 補測（60/60 那 14 條）另一個 commit。

完成定義（⑫）：uv run pytest 286 passed／uv run ruff check . 乾淨／verify_f134.py **60/60**（原 46 條＋⑧ 補測 14 條）／
verify_f86 38/38、verify_f87 38/38、verify_f59 ALL PASS、verify_f58 ALL PASS、verify_f113 15/15（⑪ 的不得回歸清單全數重跑）／
APP_VERSION 與 CACHE_NAME 同為 v145／dev APK release-dev/lift-log-v145.apk 已建置並裝上真機。
⚠ 這些腳本在預設 cp950 主控台會 UnicodeEncodeError 中斷，須 PYTHONUTF8=1；該缺陷已 DEFER 為 F138。

① 5 點、1 條 polyline、min(點頂)=311.0 ≥ 容器頂 289.0（最大值那點未被裁切）；bestSet() 函式體逐字未變（git diff 8264630 63962a4 對照）。
② .line-flag=4，第 3 筆比上筆輕正確不標（running 累進判定不變）。
③ .bars-max 顯示 75 kg、.bars-foot 含「月」與「個人紀錄」，兩處斷言都保留。
④ 三項與 .hist-card 逐項比對一致；驗收者另寫 page.on("request") 監控腳本，點選／換點／關閉三動作 request 數皆 0（不重新查 API）。
⑤ 四條路徑都有實測斷言：換點不累積（.line-tip=1）、再點同一點關閉、點 .bars-head 空白處關閉、切區間藥丸清除。
⑥ 320px／8 點：圖寬 252px、門檻 min(44, 252/8)=31.5，實測頭 32.0px／中段 32.0px／尾 31.0px；落在兩點之間仍選最近點。
   **移除修法會 fail 的證明**：x 公式由 ((i+0.5)/n)*100 改回 (i/(n-1))*100 → 頭尾命中寬掉到 18-19px，紅掉頭尾兩條斷言（主 session 實跑過）。
⑦ 最左點 tip 左緣 34≥34、最右點 tip 右緣 286≤286，兩端都完整在容器內。
⑧ (a) 0 筆：文字「這個區間內沒有紀錄」、.line-svg/.line-pt/.line-path 皆 0、點卡片不產生 tip（導覽走訓練記錄頁的「動作表現」捷徑，
      因「表現」頁籤的清單走 has_data=true 過濾，全無紀錄的動作不會出現在那裡）。
   (b) 同日兩場：x0=114.50 vs x1=275.50、圓心距 161.00px，兩點 aria-label 同日期。
   (c) 50 點：.line-pt=50（不省略）、未選中點直徑 8.00px＝N=8 基準 8.00px（未放大）、scrollWidth 323 ≤ clientWidth+1 323（無橫向捲動）。
   (d) **結構上不可觸發**，兩條 API 邊界探測寫進腳本：POST 缺 weight_kg 被擋在 400（schema 必填、DB 非 nullable）；
      整場的組軟刪後該場整個從 sessions 消失（不是 sets:[] 殘影，sessions 內 workout_id 清單為 []）。
      故 chartPoints() 的跳過分支是純防禦性程式碼，現行公開 API 無輸入路徑可達——不是「測到但沒處理」。
⑨ .line-chart svg.line-svg 為 inline SVG；package.json 依賴仍 4 顆，git diff 對 package.json 完全無輸出。
⑩ F86 superseded_by 已寫入（83de4b7），F86 ⑤ 原文完整保留、其餘條文與 status（本來就 failing）未動；verify_f86 的 .bars-max／.bars-foot 斷言保留。
⑪ F35 純後端未動（pytest 286 全過）、F86 其餘 38/38、F59／F58 ALL PASS、F113 15/15。

**真機 320dp（主 session 本人，Galaxy RF8NB0BSEFE，adb wm density 720 → 320dp 寬，事後還原成 Ryan 原本的 override 600）**：
折線圖正常出現（25 點、PR 獎盃 2 顆、右上 42.5 kg、下緣 5月／8月）；最左點命中 → 05/09 · 3 組 · 40kg × 8 靠左對齊未裁切；
最右點**故意點偏**（往上偏約 200 裝置 px）仍命中 → 08/07 · 5 組 · 40kg × 5 靠右對齊未裁切，且與下方歷史清單同一天那張卡（5 組、最佳 40×5）一致；
換點不累積第二個框；再點同一點關閉。
