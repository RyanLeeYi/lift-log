# F146 — Web 多帳號化

實作日：2026-08-13（夜間）。重驗：2026-08-14（F154 落地後，見下方「重驗」段）。
**狀態：passing**——2026-08-14 獨立驗收（fresh context，同模型非跨模型）逐條 11 項全 pass，
ACCEPT；驗收前後 `git status` 一致，`.harness/failures.jsonl` 未新增。

**2026-08-14 更正**：昨晚這份檔案漏了一條 acceptance 明文要求。F146 的 acceptance 寫「依 PRD
**R1、R5**」，而 PRD R5（第 89 行）要求「Web 建立的 mutation 與 MCP 建立的 mutation 都必須進
**同一 change log**」、R6（第 96 行）明文禁止「繞過同步版本的第二條 server 寫入路徑」。實際查證：
`SyncChange` 只有 `app/services/sync.py` 在寫，**所有 REST／Web domain 寫入都沒進 change log**，
而且 `sync.py` 從頭到尾沒碰過 domain models、domain 表也沒有 `sync_id`／`version` 欄位——
同一個 user 的 data DB 裡是**兩份互不相干的資料**（Android 同步的在 `sync_entities`，
REST／MCP 的在 domain 表）。這不是 F146 漏一行 hook，是同步層與 domain 層根本還沒合一。

Ryan 於 2026-08-14 裁決：**domain 表當 SSOT**（見 DECISIONS D16），且這塊拆成獨立 feature，
不塞進 F146 的凍結範圍。Playwright 的「CSRF 拒絕／兩帳號隔離」則改由 server 端測試負責
（acceptance 已於 2026-08-14 重新簽核）。

commits：`906e367`（CSRF token 推導）、`014b48d`（web session client）、
`1365787`（登入／登出／離線畫面）、`8d2dcaa`（既有 e2e helper 對齊新畫面）。

## 做了什麼

**server**

- 既有（F142 時期）：Secure / HttpOnly / SameSite=lax cookie、CSRF 比對、domain API 綁 session
  user、登出撤 session、cookie 不能當 Bearer 重放。CORS 是固定白名單且沒開 credentials，
  所以 CSRF token 不會被跨站讀走。
- 新增：**CSRF token 改由 `hmac(LIFTLOG_TOKEN, session_id)` 推導**，`GET /api/auth/session`
  對 web session 回傳它。原本是隨機值只存 hash——回應送出去就再也拿不回來，網頁一重整
  cookie 還在、token 沒了，**所有寫入 403**。
  第一版寫成「每次讀 session 就換一顆」，當場打爆既有的 logout 測試（換發＝讓對方手上那顆
  失效），才改成推導值。Android session 不發 CSRF token，有測試釘住。

**前端**

- `auth.js`：`restoreWebSession` / `signInWithGoogleWeb` / `signOutWeb` / `getWebCsrfToken`。
  網頁不存 access token，只留 CSRF token；GSI script 在按下登入時才動態載入（不進 index.html，
  APK 因此不會多一個外部相依）。**503 不當成未登入**——那會把服務中斷畫成「請重新登入」。
- `api.js` `authHeaders()`：有 web session 走 cookie＋`X-CSRF-Token`；沒有才退回舊的 Bearer。
  兩條路併存是刻意的，理由見下。
- setup 畫面：Google 登入為主要動作，API token 欄位留作備援；設定頁加登出。
- 開站時登入服務不可用 → 專用的離線訊息。**不能用 `state.error`**：頂層的
  `guard(confirmActiveWorkout)` 會在啟動時把它清成 null，訊息會憑空消失（實測踩到）。

## 驗證

| 指令 | 結果 |
|---|---|
| `uv run pytest -q` | exit 0（含新增 2 條 web CSRF 測試） |
| `uv run ruff check .` | All checks passed |
| `node --test tests/js/auth.test.js` | 10/10（新增 5 條 web session） |
| `uv run python tests/e2e/verify_f146.py` | PASS（登入 client=web／CSRF header／無 token 外洩／登出／離線畫面） |
| 既有 web e2e 回歸 | `verify_f48` `f93` `f101` `f102` 全過 |

**既有 e2e 的一處必要調整**：`setup_and_home()` 原本點「第一顆按鈕」，而 setup 的第一顆
現在是 Google 登入。改成指名點「連線」，一處修好、所有 importer 一起好。

⚠ `verify_f61` / `f81` / `f110` 失敗，但**在改動前的 commit `62705ae` 上就已經失敗**
（都卡在 native 模擬路徑的 `wait_for_selector("input")`）。用臨時 worktree 逐支比對過，
不是本次造成的回歸——但這三支代表 native 模擬的 e2e 目前是壞的，值得單獨開一條處理。

## 為什麼保留舊的 API token 路徑

acceptance 要求「現有 Web 行為不回歸」。舊的單一 token 是 **60+ 支既有 e2e 的入口**，
server 端也還留著 legacy 判定（`app/api/deps.py::_is_legacy_request`）。直接砍掉等於一次
廢掉所有既有 Web 回歸驗證。**這是我的判斷，需要 Ryan 確認**：如果公開版要求「只能 Google
登入」，那要另外排一條 feature 把 legacy 路徑與相關 e2e 一起收掉。

## 2026-08-14 重驗（F154 落地後）

F154 已讓 REST／Web／batch／MCP 的 domain 寫入全部進同一份 change log，R5 的缺口關閉。
本輪在乾淨工作樹上重跑全部關卡：

| 指令 | 結果 |
|---|---|
| `uv run ruff check .` | All checks passed |
| `uv run pytest -q` | 373 passed，exit 0 |
| `uv run python tests/e2e/verify_f146.py` | PASS（web cookie session／CSRF header／logout／outage） |
| `node --test tests/js/auth.test.js` | 10/10 |
| 既有 web e2e 回歸 `verify_f48` `f93` `f101` `f102` | 全 PASS |
| server 端 CSRF 拒絕與跨帳號隔離 | `tests/test_auth.py`、`tests/test_user_isolation.py` 共 32 passed |
| R5 跨路徑（domain ↔ sync change log） | `tests/test_sync_domain_bridge.py` 9 條全過 |

逐條對照凍結 acceptance：cookie 屬性、CSRF 帶送與拒絕、domain API 綁 session user、登出清 session、
server 不可用時明確狀態且不寫本地假資料、Web 行為不回歸、R5 同一 change log——皆有對應證據。

## 獨立驗收補充查證（2026-08-14）

驗收者不只複跑指令，另外自行查證了兩項本文件沒宣稱的事：

- **R6 沒有第三條寫入路徑**：`app/api/` 與 `app/mcp.py` 內無 `session.add(` / `execute(` 直接寫入；
  `app/services/` 只有 `projection.py:271` 一處 raw 刪除（`TemplateExercise` 子表，非頂層 sync entity）。
  `PushSubscription` 不在 `ENTITY_MODELS` 內（裝置推播端點，非訓練資料），不負 change log 義務。
- **Web 不混用 Android local store**：所有 native LocalStore／outbox bridge 呼叫都被 `isNativeApp()`
  守門（`app/static/js/app.js:315,389,442,807,828,869,955,3120,3355` 等），web 分支不觸碰。

## 仍待 Ryan 裁決（不阻擋本條 acceptance）

1. **legacy 單一 API token 路徑**（`app/api/deps.py::_is_legacy_request`）照留。凍結 acceptance 要求
   「現有 Web 行為不回歸」，砍掉會一次廢掉 60+ 支既有 e2e。但 PRD 頂層非目標明文「身分只採
   Google Sign-In」，兩者字面衝突；該路徑不經 CSRF／session-user 綁定，不過走同一組 service →
   change log（未繞過 R5／R6）。公開版若要「只能 Google 登入」，另開一條 feature 把它與相關 e2e
   一起收掉。
2. **Web 端 IndexedDB 離線佇列仍在**（`app.js:2476` `enqueueSet`；`isOffline()` 只判斷 `status===0`
   真斷網，不分 native／web）。與 envelope 非目標「不做 Web 完整離線」字面有出入，但行為誠實
   （標「待同步 N 筆」，不偽裝已同步），且是 F5 時代就有的既有 PWA 行為，落在「不回歸」條款下。
