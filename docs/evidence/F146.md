# F146 — Web 多帳號化

實作日：2026-08-13（夜間）。**狀態仍為 failing**：acceptance 要求的 Playwright「CSRF 拒絕」與
「兩帳號隔離」在瀏覽器層做不出來，缺一個決定（見最後一節）。

commits：`906e367`（CSRF token 推導）、`014b48d`（web session client）、
`1365787`（登入／登出／離線畫面）、`8d2dcaa`（既有 e2e helper 對齊新畫面）。

## 做了什麼

**server**

- 既有（F142 時期）：Secure / HttpOnly / SameSite=lax cookie、CSRF 比對、domain API 綁 session
  user、登出撤 session、cookie 不能當 Bearer 重放。CORS 是固定白名單且沒開 credentials，
  所以 CSRF token 不會被跨站讀走。
- 新增：**CSRF token 改由 `hmac(LIFTLOG_TOKEN, session_id)` 推導**，`GET /api/auth/session`
  對 web session 回傳它。原本是隨機值只存 hash——回應送出去就再也拿不回來，網頁一重整
  cookie 還在、token 沒了，**所有寫入 403**。
  第一版寫成「每次讀 session 就換一顆」，當場打爆既有的 logout 測試（換發＝讓對方手上那顆
  失效），才改成推導值。Android session 不發 CSRF token，有測試釘住。

**前端**

- `auth.js`：`restoreWebSession` / `signInWithGoogleWeb` / `signOutWeb` / `getWebCsrfToken`。
  網頁不存 access token，只留 CSRF token；GSI script 在按下登入時才動態載入（不進 index.html，
  APK 因此不會多一個外部相依）。**503 不當成未登入**——那會把服務中斷畫成「請重新登入」。
- `api.js` `authHeaders()`：有 web session 走 cookie＋`X-CSRF-Token`；沒有才退回舊的 Bearer。
  兩條路併存是刻意的，理由見下。
- setup 畫面：Google 登入為主要動作，API token 欄位留作備援；設定頁加登出。
- 開站時登入服務不可用 → 專用的離線訊息。**不能用 `state.error`**：頂層的
  `guard(confirmActiveWorkout)` 會在啟動時把它清成 null，訊息會憑空消失（實測踩到）。

## 驗證

| 指令 | 結果 |
|---|---|
| `uv run pytest -q` | exit 0（含新增 2 條 web CSRF 測試） |
| `uv run ruff check .` | All checks passed |
| `node --test tests/js/auth.test.js` | 10/10（新增 5 條 web session） |
| `uv run python tests/e2e/verify_f146.py` | PASS（登入 client=web／CSRF header／無 token 外洩／登出／離線畫面） |
| 既有 web e2e 回歸 | `verify_f48` `f93` `f101` `f102` 全過 |

**既有 e2e 的一處必要調整**：`setup_and_home()` 原本點「第一顆按鈕」，而 setup 的第一顆
現在是 Google 登入。改成指名點「連線」，一處修好、所有 importer 一起好。

⚠ `verify_f61` / `f81` / `f110` 失敗，但**在改動前的 commit `62705ae` 上就已經失敗**
（都卡在 native 模擬路徑的 `wait_for_selector("input")`）。用臨時 worktree 逐支比對過，
不是本次造成的回歸——但這三支代表 native 模擬的 e2e 目前是壞的，值得單獨開一條處理。

## 為什麼保留舊的 API token 路徑

acceptance 要求「現有 Web 行為不回歸」。舊的單一 token 是 **60+ 支既有 e2e 的入口**，
server 端也還留著 legacy 判定（`app/api/deps.py::_is_legacy_request`）。直接砍掉等於一次
廢掉所有既有 Web 回歸驗證。**這是我的判斷，需要 Ryan 確認**：如果公開版要求「只能 Google
登入」，那要另外排一條 feature 把 legacy 路徑與相關 e2e 一起收掉。

## 未完成（為什麼還不能改 passing）

acceptance：「Playwright 驗登入/登出、**CSRF**、**兩帳號隔離**與 outage」。

- 登入／登出／outage：`verify_f146.py` 已驗。
- CSRF **header 有沒有帶**：已驗。但「**帶錯 CSRF 會被拒**」與「**兩個 Google 帳號互相看不到
  資料**」需要**真的 server session**，而真 session 需要通過 Google 憑證驗證——瀏覽器測試裡
  沒辦法造出來。目前這兩件事由 pytest 釘住（`test_auth.py` 的 CSRF 403、
  `test_user_isolation.py` 的跨帳號隔離），但那不是 acceptance 寫的 Playwright。

要 Ryan 選一條：

1. **加一個測試用的 Google verifier 注入點**（例如只在 `LIFTLOG_ENV=test` 時可用的假驗證器），
   讓 Playwright 能建立兩個真 session，把 CSRF 拒絕與帳號隔離真的走一遍。
   代價：production code 多一條測試專用路徑，**本身就是安全風險**，要很小心地關好。
2. **修改 acceptance**：Playwright 驗登入／登出／outage 與 CSRF header；CSRF 拒絕與帳號隔離
   由 server 端測試負責，並寫明理由。要重新簽核。

我不自己決定——第 1 條是在認證路徑上開後門，那不是實作細節。
