# F158 MCP token 管理介面與到期／唯讀

## 三段落地
1. 欄位與到期：`ba4d565`（2026-08-17）——`expires_at`／`read_only`、預設 90 天、到期與撤銷同一條 401、舊庫 ALTER
2. 授權邊界：`9d05e0e`（2026-08-19，主 session）——`resolve_token` 回整列，verifier 把 `read_only` 帶進 `AccessToken.scopes`；三個寫入 tool 入口 `_write_denied()`；紅→綠已驗
3. UI 與 API 欄位：`1ec40db`（2026-08-19，executor worker）——`mcpTokenSection`、`api.{list,create,revoke}McpTokens`、`tests/test_mcp_tokens_api.py`、`tests/e2e/verify_f158.py`
4. code review 修正：`cdee86b`——載入失敗緊迴圈、`expires_in_days` 上限 3650、建立後重抓列表

## 獨立驗收（2026-08-19，acceptance-verifier，fresh context 同模型）
1–8 全 pass；額外兩個 P2（與 `/code-review` 同結論）在 HEAD `880c454` 重現，修正 `cdee86b` 合併後 Recheck：
`pytest tests/test_mcp_tokens_api.py tests/test_mcp.py` 35 passed；`verify_f158.py` 27/27（含「GET 500 時請求數 <=2 且區塊仍渲染」）。
`.harness/failures.jsonl` 兩筆 verify 紀錄對應此處已解。

`/code-review` 第 4 條（api.js 六處重複的 native/web token preamble）→ DEFER（P3，重構，不在 acceptance 內）。

## 完成定義（驗收者實跑）
ruff 全綠；`uv run pytest` 447 passed；`verify_f158.py` 25/25（修正後 27/27）；`verify_f148.py` PASS。
第 3、5 條另以獨立 httpx／MCP client 腳本實打確認。

## 第 9 條（APK）
2026-08-19 出 v155（commit `15adc46` 升號；`npx cap sync android` → `gradlew assembleRelease`），
`unzip -p … assets/public/js/state.js` 確認 `APP_VERSION = "v155"`，已複製到 `G:\我的雲端硬碟\lift-log-apk\lift-log-v155-F158-F136.apk`（10.7MB）。
