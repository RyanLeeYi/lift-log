# F153 — app 內建對話（envelope E1 整體驗收）

## 2026-08-27（headless 場）：三個 slice 已 passing，對照工具就緒，卡在 LLM key

**slice 狀態**：F163／F164／F165 皆 `passing` 並已歸檔（`docs/archive/features.jsonl`）。
`uv run pytest tests/test_chat.py tests/test_mcp.py -q` → 48 passed；
`uv run python tests/e2e/verify_f165.py` → 27/27 passed；全庫 431 passed、ruff 全過。

**對照工具（本場新增）**：`tests/e2e/verify_f153.py`。
上一場放在 `scratchpad/f153_compare2.py` 的版本已隨未進版控的 scratchpad 消失，
因此改放進版控的 e2e 目錄，沿用既有 `verify_f*.py` 命名慣例。

它把 frozen acceptance 的驗證方式做成可重跑的對照：

- **外部路徑**：腳本自己驅動 Anthropic Messages API，tool_use 一律經 **`/mcp` HTTP 端點**
  執行（真 bearer 驗證、真 per-user DB routing）——外部 Claude／ChatGPT 走的就是這條。
- **內建路徑**：`POST /api/chat/` → 取得 `pending_write`（第一段零寫入）→ 帶回同一份才寫入。
- 兩條路徑各綁一個乾淨 user 與各自的 DB，比對時忽略 id／時間戳／client_uuid。
- 刻意**不 mock 上游**：mock 掉就證明不了兩條路徑的模型行為一致。

**已驗證到的邊界**：用假 key 跑過一次，登入、動作表種子、MCP token 核發、`/mcp` bearer 驗證、
`list_tools()` 全部通過，唯一失敗點是上游回 `401 API key is invalid`——正是預期的卡點。
（過程中修掉兩個真問題：新帳號預設動作表已含臥推導致種子撞名；Windows 暫存目錄
因 SQLite 連線未釋放而清不掉。另補上 `lifespan_context`，否則 `/mcp` 的 session manager 未初始化。）

## BLOCKED：需 Ryan 補 `LIFTLOG_LLM_API_KEY`

`.env` 的 `LIFTLOG_LLM_API_KEY` 目前是空字串。key 一補上，跑：

```
uv run python tests/e2e/verify_f153.py
```

四條判定（外部確實寫入／內建先回 pending_write／確認後才寫且附 dry-run 摘要／
兩條路徑資料列相同）全綠即可依 frozen acceptance 改 passing。

附註：本場 session 的 lift-log remote MCP server 連線失敗（`AUTH_HEADER_REJECTED`，HTTP 401
invalid_token），所以也無法用「真的外部 Claude 打正式站」這條替代路徑取證。
