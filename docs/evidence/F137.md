# F137 — E2E 腳本寫死主工作區絕對路徑，在 worktree 裡測到錯的程式碼

① 兩處指名的 REPO 已改：verify_f58.py:27、verify_f59.py:39
   `REPO = Path(r"C:/Users/user/OneDrive/Desktop/SideProject/lift-log")` → `REPO = Path(__file__).resolve().parents[2]`

② 掃描結果**不是只有那兩處**：同一個病灶還在另外 10 支 —— verify_f49／f50／f51／f52／f53／f54／f55／f56／f57／f60，
   已用同一修法一併改掉（②「一併改成相對推導」的條文本身涵蓋這個分支）。合計 12 檔、每檔僅動 REPO 那一行
   （git diff --stat：12 files changed, 12 insertions(+), 12 deletions(-)）。
   除 REPO 外沒有其他寫死絕對路徑：DB 路徑一律 tempfile.gettempdir()，verify_f60 唯一的截圖輸出也是
   Path(tempfile.gettempdir()) / f"f60_{w}x{h}.png"。主 session 事後自己再 grep 一次 tests/e2e/ 全目錄
   （C:/Users／OneDrive／SideProject／drive-letter 四種樣式），只剩 http:// URL，無檔案系統絕對路徑殘留。

③ **前後對照（修好了，不只是還能跑）**
   執行者在臨時 worktree 內把 state.js 的 APP_VERSION 改成 v999：
   - BEFORE（worktree 內未修改版腳本）：verify_f58 → `tag='v145'`、verify_f59 → `tag='v145'`，**且兩支都 ALL PASS**
     ——這正是這個 feature 要修的那一半危險：讀到主工作區的舊碼卻回報通過（假通過）。
   - AFTER（worktree 內已修改版腳本）：verify_f58 → `tag='v999'`、verify_f59 → `tag='v999'`，看得到 worktree 自己的變更。

   **主 session 獨立重現**（不採信執行者的敘述）：另開 worktree（HEAD=21e4509）、只把修好的 verify_f58.py 複製進去、
   worktree 內把 APP_VERSION 改成 v777 但**故意不同步** sw.js 的 CACHE_NAME →
   `[FAIL] ⑧ APP_VERSION 與 sw.js CACHE_NAME 同步遞增（兩處一致，≥v59） | tag='v777'`。
   讀到 v777（worktree 自己的值，不是主工作區的 v145）**而且正確地為版號不同步而紅**——同時證明兩件事：
   路徑推導修好了、斷言仍然有效。worktree 與 tmp/f137-check 分支事後已移除乾淨（git worktree list 只剩主工作區）。

④ 主工作區重跑（主 session 本人）：verify_f58 ALL PASS、verify_f59 ALL PASS、verify_f67 20/20、
   verify_f49 ALL PASS、verify_f60 ALL PASS。

⑤ uv run ruff check . → All checks passed!。純測試基礎設施改動，未升版號、未出 APK（條文明文不需要）。
   斷言內容與涵蓋範圍未動、未抽 conftest／共用 helper（non_goals 遵守）。
