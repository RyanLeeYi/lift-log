# F111 — 組列清單超過兩組時至少顯示兩列（取消矮螢幕降一列）

（2026-08-01，v120。verify_f99 19/19 ＋ 真機 SM-N9750 dev v120。）

改動只有一行 CSS：`@media (max-height: 840px)` 裡的 `--done-rows-visible: 1` 拿掉
（內距的 12px 覆寫保留）。列數不再隨螢幕高度降級。

量測（384×727，Ryan 實機）：
  修正前 1 組 68px｜2 組 125px｜**3 組 69px** ← 記到第三組時在眼前縮掉一半
  修正後 1 組 68px｜2 組 125px｜**3 組 126px**｜6 組 126px，主按鈕底 711/727、頁面 0 溢出

真機（截圖 f111-02）：三組時清單顯示兩列（#3、#2，下緣淡出表示還有 #1）。

verify_f99 19/19，新增兩類斷言：
  ① 「3 筆的高度不得小於 2 筆」——**原本的測試抓不到這個 bug**，因為它只驗
     「高度是列高的整數倍」，而 69px 是 1 列的整數倍、完全合法。
     驗的要是「不得比上一個狀態更矮」，不是「符合某個公式」。
  ② 384×727 與 390×640 都要 ≥2 列。

回歸：verify_f84 48/50（僅剩既有的 F109，360×640 休息態 9px，**數值未變**，
本條沒有讓小螢幕更糟）。ruff clean。
