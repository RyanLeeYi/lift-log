# F110 — 「回 app 記下一組」要跳到那輪休息所屬動作的計時頁

（2026-07-31，v118。verify_f110.py 11/11 ＋ 真機 SM-N9750 dev v118。）

已完成：
- 原生 `RestOverlay` 的主按鈕在拉起 app 之後多送一個 `emit("focus")`。
  導頁交前端做——哪一頁、動作物件在哪、離線怎麼退，都是前端才知道的事（原生只有一個 id）。
  與 F103 ⑤ 同一個分寸：原生回報「使用者按了什麼」，狀態變更在 JS。
  `compileDevReleaseJavaWithJavac` 通過。
- 前端 `focusRestExercise()`：從 `pickerExercises` 找出物件後呼叫 `pickExercise()`
  （它自己會切到 logger 並重繪）。**刻意不碰休息本身**——導頁若走到
  finish()／stopRestTimer() 就會把這輪收掉，而 F103 ① 明訂這顆鈕不是結束的入口。
  focus 的處理放在 `restStartedAt` 檢查**之前**：浮動視窗按過停止之後前端那份倒數已收掉，
  但那輪還在原生那邊撐著，這顆鈕當然還要能把人帶回去。

verify_f110.py **11/11**（只涵蓋導頁這一半，拉起 app 是原生的事、Playwright 碰不到）：
  ② 從別的動作的計時頁、從菜單，都要跳到所屬動作
  ③ 到了之後是正常休息態（卡片在、回報卡片可見 → 視窗會消失）
  ④ 兩條退路：沒有這輪休息時不跳、已經在那一頁時不重跳（不洗掉畫面狀態）
  ⑤ **反面**：導頁不得結束這輪休息（沒有回送原生停止指令）——兩個情境各驗一次

回歸：verify_f108 20/20、f103 16/16、f69 21/21、f70 18/18。ruff clean。

真機（截圖 f110-01～04，v118）：
  深蹲記一組 → 休息中 → 切到硬舉的計時頁（app 全程開著，沒有切出去）→
  浮動視窗以收合態顯示在硬舉頁上，展開後提示是「**槓鈴深蹲 · 第 2 組**」（歸屬正確，F108 ④）→
  按「回 app 記下一組」→ **畫面跳到槓鈴深蹲的計時頁**、休息卡在、倒數連續（00:35 仍在跑）、
  視窗消失、那輪休息**沒有被結束**（狀態字仍是「休息一下」、主按鈕是「繼續下一組」）。
  結束訓練後 RestTimerService 歸 0。

這正是 Ryan 指出的情境（「如果是在 app 裡面」）——在此之前 SINGLE_TOP 只會把既有的
Activity 叫回來，人停在硬舉頁不動。
