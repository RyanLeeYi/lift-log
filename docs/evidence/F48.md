# F48 — 課表三處清單超過兩項改固定高度捲動

2026-07-25：實作者 E2E `verify_f48_own.py` 11/11 PASS（真實滑鼠滾輪＋非邊界值 200/40，點擊改 dispatchEvent 繞開 Playwright auto-scroll）；pytest 189 passed、ruff clean；回歸 verify_f42 19/19、verify_f43 全綠。codex-review 兩輪：第一輪 1 條 P2（menuScrollTop 跨訓練沒清，換課表會沿用舊偏移把前幾個動作藏起來）已修（startWorkout 歸零＋課表頁進場歸零），第二輪無 findings。acceptance-verifier 獨立重驗 ①–⑦ 全 PASS＋三條反向情境（換訓練/重進頁面/首次進菜單皆從頂端）。
重要修正：首版靠 onscroll 記錄捲動位置，但節點被重繪拆掉時瀏覽器補送一次 scrollTop=0 的 scroll 事件，把記錄覆寫成 0＝還原到頂端；實作者首版 E2E 用 e.scrollTop=e.scrollHeight（最大值）與失敗態結果重合而假 PASS，由 acceptance-verifier 用真實滾輪抓到。根治＝拿掉鏡射，改在 render() 開頭從 DOM 讀舊節點位置（app.js captureScrollPositions／templates.js captureTemplateListScroll）。已記入 .harness/failures.jsonl。
