# F88 編輯課表改版（動作卡 ＋ 圓鈕控制列）

## 狀態
- 畫面實作：2026-07-30 起，後續 F96／F97／F98 疊加（見條目 `superseded_by`）
- ⑩ 隔離區腳本翻新：2026-08-19 commit `8b307fa`（executor worker，worktree），verify_f48–f52 改用 `verify_f67` 共用 helper
- 獨立驗收：2026-08-19 `acceptance-verifier`（fresh context，同模型），①–⑩ 全 pass，最高 severity 無

## 完成定義輸出（驗收者實跑）
| 指令 | 結果 |
|---|---|
| `uv run ruff check .` | All checks passed |
| `uv run pytest tests/test_api_templates.py` | 29 passed |
| `verify_f88.py` | 31/31 |
| `verify_f48`–`f52` | 11/11、17/17、14/14、7/7、7/7 |
| `verify_f97.py`／`verify_f98.py`（回歸） | 36/36／30/30 |

截圖：`F88_edit_collapsed_390x844.png`、`F88_edit_expanded_390x844.png`

## 驗收備註
- ⑥ 底部第三顆按鈕文案是 F51 凍結的「← 課表列表」而非字面「取消」；F88 明文要求 F51 不回歸，兩份 E2E 皆未斷言字面「取消」，判 pass。未來寫 acceptance 時把文案寫成「沿用 F51 文案」可消歧
- ⑩ 五支本來就沒有 `.home-start`（F81 時已換成 `wait_home`／`start_from_home`），未強插未使用的呼叫
