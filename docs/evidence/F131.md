# F131 — 背景記組即時寫入（原生直接打 API，取代排入→回 app 才寫）

真機 SM_N9750（dev APK v140→v143）逐條，DB 為 liftlog-dev.db。
⑩-1 背景按下不回 app → sets.id=417、set_number=9（server 指派）、rest_seconds=146（2026-08-04）。
⑩-2 回 app 後只有一筆（id=417；驗收者於 workout 77 再驗一次）。
⑩-3 背景按下→立刻按 ✕ → sets.id=419、set_number=10 仍在，server log 對應 POST 201（2026-08-05）。
⑩-4 飛航下按兩組→開網路→不碰 app，20 秒內 id=421(set 12)、id=422(set 13) 依序落地，NetworkCallback.onAvailable 生效。
⑩-5 背景按下→force-stop→重開 → client_uuid=0f316374… 仍只有 id=423 一列，無重複。
⑩-6 前景行為與 v139 並排比對（2026-08-06，v139 dev APK 由 b9ee5f6 重建、SITE=dev、同 release 憑證簽章）：
     v139 記一組 → workout 79 建立、sets.id=440、set_number=1、UI 顯示 #1、標題「第 2 組」、倒數 30s；
     v143 記一組 → sets.id=441、set_number=2、UI 顯示 #2、標題「第 3 組」、倒數 30s；
     v139 原位編輯 reps 8→9、set_number 不變；v143 原位編輯 reps 9→10、set_number 不變。
     唯一差異＝組號來源（v139 由 JS 自算、v143 由 server 指派），即 (b) 改判的刻意變更，見 spec_feedback ②。
⑩-7 token 明文次數 0、鍵名亦為密文（debuggable build 取證後已還原；執行者＝取證者，非獨立）。
⑧ 永久失敗態（2026-08-06）：workout 78 從 DB 移除製造 404（curl 覆核 POST /api/workouts/78/sets → 404），
   背景按「完成這組」→ 浮動視窗紅字「沒記到 1 組——回 app 處理」；回 app 後出現既有橫幅
   「⚠ 同步失敗 1 組（點此捨棄）」，未新增任何 UI；按捨棄後橫幅消失。截圖 s11/s12/s13。
   註：401 依 SetUploader.java:101 屬可重試而非永久失敗（codex review 時刻意改），故永久失敗態以 404 驗。
完成定義：pytest 283 passed、ruff 乾淨、init.sh exit 0（2026-08-05 codex-verify 當輪）。
組號回歸：全 dev DB 未軟刪重複組號 0 筆；prod 舊撞號（workout 33／exercise 30）已於 2026-08-06 依 created_at 重編為 1..5，備份見 lift-log-backups。
--- 2026-08-07 獨立補驗（acceptance-verifier，非實作者；Codex 額度用盡故未用 codex-verify）---
真機 SM_N9750、dev APK v144、dev DB liftlog-dev.db，動工前備份 lift-log-backups/liftlog-dev.db.bak-20260807-0906。
⑥ 永久性 4xx 停止重試：刪除 workout 81，curl 覆核 POST /api/workouts/81/sets → 404；背景按「完成這組」後
   sets 表無任何新列（僅原有 id 455 未變動），未被誤判成功。SetUploader.java:108-113 非 401/5xx/408/429/timeout
   一律 markFailed，flushBlocking() 只處理 STATUS_PENDING，failed 條目結構性不再重試。
⑥ outbox 不得被 ✕／停止／結束訓練清空：飛航下停止→「完成這組」入 outbox（視窗「離線，已排隊 1 組」）→再按停止
   （計數仍 1）→按 ✕（觸發 ACTION_STOP，計數仍 1，回 app 顯示「⏳ 待同步 1 組」）→結束訓練（仍離線，橫幅不變）
   →關飛航回前景 flush → 該組成功落地 sets.id=454（workout 80、set_number=2）。全程計數未減少。
   碼側佐證：RestTimerService 的 ACTION_STOP／ACTION_HALT 分支都不呼叫 SetOutbox 清除；discardFailed() 只由
   RestTimerPlugin.discardFailedOutbox 呼叫，該路徑只掛在 app.js:376「點此捨棄」；endWorkout()（app.js:1029）不清 outbox。
⑦ 回 app 對帳為全量重抓：重啟 dev server 導出 log（PID 27416→33564，健康檢查 200，prod 8137 未動）；
   背景切前景觸發 visibilitychange → server log 出現 GET /api/workouts/81 → 對應 refetchActiveWorkoutSets()
   的 api.workoutDetail(workoutId)，單一資源全量端點，非增量查詢。
⑧ 永久失敗態 UI（獨立重驗）：同 404 場景，浮動視窗紅字「沒記到 1 組——回 app 處理」（與「離線已排隊」文案明確兩分）；
   回 app 後既有橫幅「⚠ 同步失敗 1 組（點此捨棄）」，未新增任何 UI 元件；按捨棄後橫幅消失、outbox 計數歸零。
判定：本輪四條全 pass、零 fail、無規格問題（附錄 A 判準已足夠具體）。
⚠ 既有已驗項目（①–⑤、⑥-a、⑨、⑩-1～⑩-7）沿用前幾場紀錄，非本輪重驗；⑩-7 仍是執行者＝取證者。
⚠ 本 feature 的 review 與驗收皆為 Claude（跨 session、跨 context，非跨模型）——Codex 額度用盡的退路。

