# F70 — 休息倒數跨畫面存活

（2026-07-28 v77）自動化：verify_f70.py 18/18（走真實 UI 流程並回頭打 API 查寫進資料庫的 rest_seconds）；pytest 205、ruff clean、既有 E2E 全綠。
實作：換動作不再 stopRestTimer（①）；restTargetSeconds 在休息開始時快照、改秒數時同步（④——否則換動作後倒數基準會跟著新動作的參考值跳掉）；② 的三條取消路徑（繼續下一組／結束訓練／401）未動。
③ 實測：第二組寫入 rest_seconds=6s vs 實際經過 6s；第一組仍不帶。實作上不需要第二條來源——休息中按鈕一律是「繼續下一組」，所以「休息還在跑就直接記組」在 UI 上不存在（我一度加了 fallback，發現是死碼後移除）。
⑥ 模擬器實測：休息中換動作 → 浮動視窗出現在選動作頁、倒數不中斷；回計時頁 → 視窗消失、REST 卡片依實際經過時間續算。
規格來源：F69 驗收時發現凍結的 ① 描述了不可達狀態（離開計時頁會取消休息），回報後 Ryan 決定改行為而非改規格，另立此條。
