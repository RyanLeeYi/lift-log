# F32 — 換動作後保留本次已做組數

2026-07-20 完成。state 加 doneByExercise:{exerciseId:[sets]}（納入 saveActiveWorkout/restoreActiveWorkout/clearActiveWorkout，收工清空）；pickExercise 若鏡射有組→還原本次組、hint 標「本次」而非「上次」並跳過 lastSets；logSet/deleteDoneSet/saveEditDoneSet 後 rememberDoneSets 鏡射同步。「上次」提示改查前一次訓練：last-sets 加 exclude_workout 參數（service last_sets(exclude_workout_id) 過濾掉進行中 workout），pickExercise 一律帶 state.workoutId → done-list 顯示本次組、hint 顯示前一次訓練值（查無前一次才退回本次摘要）。sw.js v33、APP_VERSION v33。codex-review 1 P1+1 P2 皆修：P1 syncQueue 補傳成功換 server id、discardFailed 捨棄失敗組都經 reconcileDoneSets 掃全鏡射同步並持久化（否則換動作復活無 id 舊 payload，刪除不刪 server、編輯撞 client_uuid 冪等）；P2 pickExercise 偵測 setCounts>0 但鏡射空（v31→v32 升級）→ workoutDetail 回填。後端 test_api_workouts 加 2 條（exclude_workout 回前一次、只有本次時回空）。E2E verify_f32.py 8/8：A0 首選顯示上次 50、T1 換動作回來保留 2 組、T2 上次顯示前一次 50（非本次 60）、T3 續接第 3 組、T4 全新動作維持第一次、T5 升級回填 done-row 3＋上次仍 50、T6 離線記錄→補傳→換回→刪除真的刪到 server（server_remaining=0）。「上次查前一次」refinement 再跑 codex-review、2 P2 已修：①resume 分支 lastSets 的 catch 只吞離線（status 0），401/5xx 重拋交 guard（否則 token 失效不導回 setup）；②pickExercise 兩處 await 後加 state.exercise!==exercise 過期守衛（await 期間已換動作/結束訓練就丟棄，不把畫面拉回 logger）。全套 pytest 175 過、ruff clean、F31 logger 回歸 4/4。
