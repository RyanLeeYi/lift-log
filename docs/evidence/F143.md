# F143 — 同步 server 協定

2026-08-11：Codex fresh-context 主驗收除 Android sandbox UNVERIFIED 外全 PASS、integrity VALID；backend 338 passed、ruff clean，unknown cursor temp DB black-box PASS。Codex review 修正 concurrent mutation lost update 與 chunked/Content-Length/CORS 邊界。第二個 targeted fresh-context Codex 以本機 SDK 補驗 Android：Android/server 共用同一實體 JSON fixtures，Gradle BUILD SUCCESSFUL、JUnit 2/2、integrity VALID。generic sync store 與既有 REST/MCP domain tables 整合 defer F146/F147；sequence reset/restore ops defer F149。
