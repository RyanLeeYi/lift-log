# F135 — 折線圖在高 N（例如 50 次）時點重疊到無法辨識

實作分兩顆 commit：a9805b8（④ 重做＝獎盃帶＋分層交錯）、a0e72d2（review 修正，v148）。

**④ 為什麼重做**：8/7 的第一版（icon 依 N 分級縮小）第一輪驗收量出 N=50／320px 下獎盃互疊 49 對
（最大 17.58px²）、蓋住鄰點 98 起。根因是舊版 verify_f135.py 只量「獎盃彼此的 x 間距」，
④ 的另一半「不得蓋住鄰點」從未被斷言，且點距小於 icon 時連斷言都不做、只印數字。
Ryan 2026-08-08 裁示照字面判 fail、重做（不改條文）。縮 icon 無解——要不重疊得縮到 5px 以下，
stroke 只剩 0.42px 等於變相省略標記，而 F134 ② 明文要求 PR 點要有獎盃 icon。

**修法**：點距容不下 icon 時，獎盃移進繪圖區上方的專用帶，依序位 i % rows 分層交錯
（rows = ceil(iconW / spacing)），繪圖區 padTop 下移到帶子底下。兩條保證是幾何必然：
帶子與資料點佔不相交的 y 區間（不可能蓋點）；同層兩個獎盃相隔 rows × 點距 ≥ icon 寬（不可能互疊）。
切換條件是「點距容不下 icon」而非寫死 N>40——中階（21–40）本來也在違反 ④（icon 9px、N=34 點距 7.4px）。

逐條實測（三情境 N=50／N=21／N=34）：
① 點徑 4.00／6.00／6.00px，與分級一致（另驗邊界 N=20→8px、N=40→6px）
② 相鄰點圓緣最小間距 1.03px（N=50）、6.00px（N=21）、1.41px（N=34），皆 ≥1px
③ 選中點三情境皆 13.00×13.00px ＋ 光暈未變
④ 獎盃互疊 **0 對**、蓋住鄰點 **0 起**（三情境）；重做前為 49 對／98 起
④-c 選中最高點時框頂緣距帶底緣 4.50px ≥ 4px 光暈（review 追加）
⑤ 頭／中／尾命中寬皆過 min(44, 圖寬/N) 門檻；x 公式與 nearestPointIndex 本次未動
⑥ verify_f134 60/60、verify_f86 38/38、verify_f113 15/15、test_exercise_history(F35) 20/20

完成定義（⑦）：uv run ruff check . 乾淨／uv run pytest tests/test_exercises.py 10 passed／
verify_f134.py 60/60 不回歸／verify_f135.py **44/44**（新增 N=34 情境；移除修法會 fail 已三度重現：
N=50 互疊 49 對、蓋鄰點 87 起、N=34 互疊 33 對）／APP_VERSION 與 CACHE_NAME 同為 v148／
dev APK release-dev/lift-log-v148.apk 已建置並裝上真機（versionName 148-dev）。

真機 320px：SM_N9750 實體寬 **1440px**，故 320dp 用 adb shell wm density **720**
（1440÷320×160；先前 handoff 記載的 540 是錯的，那是 426dp）。槓鈴臥推切「全部」＝33 筆走帶狀，
實機確認獎盃兩層交錯、彼此不重疊、不壓在資料點上，密集區點擊命中正常。驗畢已 wm density reset。

⚠ 已知未修的註解瑕疵：exercise-detail.js 的 maxRows 註解寫「n≥379」，那是 FLAG_BAND_GAP=6
（review 當下）的數字；同一顆 commit 把 gap 改為 9 後，首次 plotH<0 前移到 n=347。
行為不受影響（maxRows 對所有 n 皆生效，實測 n=41..200000 的 plotH 恆為 69px）。
不在本輪改，因為 exercise-detail.js 是 SW 快取的 shell 資產，動它要升版號、會作廢剛通過的驗收——
留給 F136（touches 完全重疊）順手改。

⚠ 本條的 review 與三輪驗收**全部是 Claude，不跨模型**。Codex 額度先是耗盡，恢復後兩度執行逾時
被中止（未產出報告，工作區確認未被修改），由 Ryan 裁示改用 Claude。這是本條已知的獨立性折扣。
