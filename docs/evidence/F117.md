# F117 — 加動作視窗在 320×420 出現雙層捲動

（2026-08-01，v132）verify_f52 **ALL PASS**。

## 先量再修

原本只知道「320×420 時視窗自己會捲」。在 verify_f52 的探針加了三個診斷欄位
（`need` / `have` / 每個子節點的高度），一次就看出全貌：

    need 346 / have 336 → **只差 10px**
    子節點：modal-head 24｜搜尋列 42｜chips 96（320 寬會排成 3 列）｜
            exercise-list 0（已經被壓到底）｜＋自訂動作 53｜確定加入 53

清單早就壓到 0 了，所以差額只能從別處來。**這三個欄位留在腳本裡沒拿掉**——
下次再有人動這個視窗，紅燈會直接告訴他是誰吃掉的，不必再猜一輪。

## 修法（③：不壓觸控目標）

差額從**非觸控的間距**擠：`@media (max-height: 480px)` 內 `.modal` 的
內距 14→10（省 8）、列間距 10→6（省 20）。
「確定加入」與「＋ 自訂動作」實測 53px，壓到 44 也還合法——但 F117 ③ 明文禁止走這條路，
沒有動它們。修完清單反而拿回 18px（0 → 18px）。

## 逐條

① 320×420 `.tpl-add-modal` 不再自捲（`self_scrolls: False`，need 336 = have 336）。
② 「確定加入」完整在視窗內（`btn_inside: True`）。
③ 觸控目標一個都沒動；搜尋框字級仍 ≥16px（verify_f52 的 review P2-2 同時綠著）。
④ 844×390 橫放不回歸（`self_scrolls: False`，清單從 18px 增為 46px）。
