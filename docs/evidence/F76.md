# F76 — emoji 圖示改為向量圖示

（2026-07-28 v81）新增 app/static/js/icons.js（Lucide 路徑內嵌，stroke=currentColor、線寬統一 2），首頁四顆入口、休息提醒、浮動計時、暫停／停止／完成、返回、編輯／刪除、同步狀態全部改向量；原生浮動視窗改用 4 個 vector drawable + setImageTintList。icons.js 已進 sw.js 離線清單（專案既有測試抓到漏列）。模擬器截圖確認 web 與 app 兩邊都正確渲染。
