# F38 — 動作詳情頁：兩入口與返回

兩入口與返回（與 F36 同批實作）：picker 每列（含今日菜單列）＋logger 標頭 📈 詳情入口，點📈進詳情頁不誤觸開始記錄；返回回來源畫面且狀態保留（picker 搜尋/部位、logger 進行中組與休息計時）。sw v37。E2E verify_f36 涵蓋兩入口進出與返回保留；codex-verify 跨模型 F38 全 pass（picker 35 列各含主鍵+📈、今日菜單 1 列有📈、logger 往返組數不變、返回保狀態、📈對比 9.32:1）。commit f08aa53。
