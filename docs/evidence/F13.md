# F13 — 靜態資產快取策略（sw.js no-cache）

2026-07-18：136 tests 全過、ruff 乾淨、codex review 無 findings。acceptance-verifier 4/4 PASS：pytest 兩條 F13 測試＋正式 8137 實回 cache-control: no-cache；公開 URL cache-bust 三連打 MISS→REVALIDATED→REVALIDATED（每次回源驗證，部署即時生效；zone 會把瀏覽器側標頭改寫 max-age=14400，但瀏覽器對 SW 主腳本更新預設繞過 HTTP cache，不影響）；/js/app.js 無 no-cache（策略不變）。已知一次性殘留：改版前的舊 /sw.js 快取條目 HIT 至 TTL 過期（~2h）或手動 purge，此後不再發生
