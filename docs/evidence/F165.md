# F165 evidence：內建對話 UI 與自填 LLM key

2026-08-22 實作（dispatch session）。

## 做了什麼

- `app/static/js/chat.js`（新檔）：對話畫面。訊息串、dry-run 摘要卡（確認／取消）、
  錯誤碼 → 人話對照表。取消**不送第二次請求**——沒有請求就沒有寫入。
- 入口走底部導覽第五顆「對話」（acceptance ① 的兩個選項擇一）。
- `app/static/js/account.js` 新增 `llmKeySection()`：設定頁填入／清除自己的 key，
  存 `localStorage['liftlog.llm_key']`，已填時標示「使用自己的 key」（④）。
- `app/static/js/api.js` 新增 `api.chat()` 與 key 存取；key 只以 `X-LLM-API-Key`
  header 夾帶，不進請求 body、不進同步。
- `sw.js`：SHELL 加 `/js/chat.js`，`CACHE_NAME` v157 → v158（⑧）。
- `state.js`：`APP_VERSION` v157 → v158（⑨）。

## 驗證

`uv run python tests/e2e/verify_f165.py` → **27/27 passed**（①-⑥ 全涵蓋）。
只攔 `/api/chat/` 模擬 LLM（⑦ 明文要求）；其餘 API 打真的本機 server，
③ 的「確認後那筆真的在」是用真的 `POST /api/workouts/batch` 落庫後，
再回日曆頁確認今天那格上色 ＋ `GET /api/workouts` 有那筆。

`uv run pytest tests/test_chat.py -q` → 12 passed；`uv run ruff check .` All checks passed；
`uv run pytest -q` 全綠（484 條）。

## APK（⑨）

```
npx cap sync android          → Sync finished
gradlew -p android assembleRelease → BUILD SUCCESSFUL
android/app/build/outputs/apk/prod/release/app-prod-release.apk (10,725,069 bytes)
→ G:\我的雲端硬碟\lift-log-apk\lift-log-v158-F165.apk
unzip -p <apk> assets/public/js/state.js | grep APP_VERSION → export const APP_VERSION = "v158";
unzip -l <apk> | grep chat.js → assets/public/js/chat.js
```

## 界線

E2E 驗的是瀏覽器這一側的畫面契約；server 端的兩段式語意、key 不落地、per-user 隔離
由 F164 的 pytest 釘住。真機安裝與實機操作未驗（headless session 無裝置）。
