# F166 — rest-alarm channel 被單獨關閉時也要擋下

## 2026-08-27（headless 場）：①–⑤ 全數驗證通過，⑥ 卡在裝置狀態

**程式（①③⑤）**：`1fc590e` 已把判定收斂到單一 channel。
`app/static/js/native-notify.js:47` → `const REST_CHANNEL_IDS = ["rest-alarm"];`，
「查不到不算被關、拋錯退回 areEnabled」的規則一字未動（同檔 :59-74）。

**④ E2E**：`uv run python tests/e2e/verify_f95.py` → **12/12 passed**，含 F166 新增四情境：

- `rest-alarm importance=0` → 顯示「關」
- 反面 `rest-timer importance=0` 但 rest-alarm 正常 → 顯示「開」
- `default importance=0`（rest-alarm 未建立）→ 顯示「開」
- 邊界：rest-alarm 尚未建立（前景服務沒跑過）不算被關

全庫回歸：`uv run pytest -q` 431 passed、`uv run ruff check .` All checks passed。

## ⑥ 真機重現：BLOCKED（需 Ryan 本人）

裝置 `RF8NB0BSEFE`（SM-N9750）已連線，且**機上 dev APK 確實含 F166 程式**——
`adb pull` 後解出 `assets/public/js/native-notify.js` 為 `REST_CHANNEL_IDS = ["rest-alarm"]`，
`state.js` 為 `v161`，`versionName=161-dev`。channel 現況也正是 ⑥ 需要的基線
（`rest-alarm` importance=4、`rest-timer`=2、`default`=3）。

擋住的是兩個裝置狀態，兩個都只有 Ryan 能解：

1. **dev app 已登出**——開啟後停在「使用 Google 登入」頁，設定頁（休息提醒開關）在登入牆後。
   點兩次登入鈕（已校正實機座標）畫面無變化、logcat 無對應事件；dev 站本身 HTTP 200 正常。
   Google 登入屬使用者身分操作，不代為執行。
2. **app 層通知權限被撤銷**——`adb shell cmd appops get com.ryanleeyi.liftlog.dev POST_NOTIFICATION`
   → `ignore`（dumpsys 亦顯示 `allowNoti=false`）。即使能進設定頁，`areEnabled()` 為 false
   會讓開關因為**錯的理由**顯示「關」，這樣的重現證明不了 F166 的判定邏輯。

補充：正式 app（`com.ryanleeyi.liftlog`）是 v160，仍是 F95 舊邏輯
（`REST_CHANNEL_IDS = ["rest-timer", "rest-alarm", "default"]`），不能拿來驗 F166。

Ryan 只需：在 dev app 登入 → 系統設定把 dev app 的通知權限打開 → 回報，即可完成 ⑥。

## 2026-08-31（headless 場，第十五場）：①–⑤ 複驗全過，⑥ 仍 BLOCKED（改為裝置未連線）

Ryan 已完成他那兩步（dev app Google 登入、系統通知總開關打開），但**手機沒有接上這台電腦**：
`adb devices` 回空清單（daemon 重啟＋`adb reconnect` 皆試過），因此無法在裝置上重現與取證。

複驗結果（程式面沒退步）：

- `uv run python tests/e2e/verify_f95.py` → **12/12 passed**（含 F166 四個新情境）
- `uv run pytest` → **491 passed**；`uv run ruff check .` → All checks passed

⑥ 只差：手機用 USB 接上並允許偵錯（或開無線偵錯提供 `adb connect` 位址）。

### ⑥ 真機重現：PASS（2026-08-31 稍晚，手機接上後補完）

裝置 `RF8NB0BSEFE`（SM-N9750），dev app `com.ryanleeyi.liftlog.dev` v161-dev，已登入
（設定頁看得到「Google 帳號／登出」）。系統「顯示通知」為**開**。

| 步驟 | 裝置狀態（`dumpsys notification`，dev 套件段） | 畫面 |
|---|---|---|
| 基線 | rest-alarm=4、rest-timer=2、default=3 | 休息提醒 **開**（`f166/01-baseline-on.png`） |
| 只關「休息時間到」 | rest-alarm=**0**、rest-timer=2、default=3 | 休息提醒 **關**（`f166/02-rest-alarm-off.png`） |
| 反面：只關「休息倒數」 | rest-alarm=4、rest-timer=**0** | 休息提醒 **開** |
| 收尾還原 | rest-alarm=4、rest-timer=2、default=3 | — |

channel 開關是在系統「應用程式通知 → 通知類別」頁上實際點的（adb 只用來導頁、點擊與讀狀態，
沒有繞過系統 UI 直接改 importance）。畫面狀態每次都是把 app 切回前景後重讀（F95 ⑤ 的前景複查路徑）。

反面情境的截圖與基線**逐位元相同**（md5 `cf2d241d2125ffbeca660e583db50d4b`），所以不另存一份檔案
——那份「沒有變化」正是要證明的事：`rest-timer` 被單獨關掉不影響顯示。

另外澄清一個舊誤判：`dumpsys notification` 裡 `AppSettings: com.ryanleeyi.liftlog.dev … allowNoti=false`
這行在本場**與畫面不符**（系統設定頁顯示「顯示通知：開」，且基線畫面確實顯示「開」，
代表 app 的 `areEnabled()` 回 true）。判斷通知權限請以設定頁與 app 實際行為為準，不要只看這行。

**結論：①–⑥ 全數通過。**
