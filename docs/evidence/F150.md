# F150 — 批次寫入 endpoint

驗收日：2026-08-13。

`POST /api/workouts/batch` 共用既有 `log_workout` service，單一 `BEGIN IMMEDIATE`
transaction 寫入一場 workout 的多筆 set；沒有另建 domain 驗證或寫入路徑。

- 全成功：201，回傳同一場 workout 與全部 sets。
- 單筆 schema 無效、未知動作（含大小寫變體）與未知課表：400，回傳
  `{index, field, message}`，且 before/after row count 不變。
- 空批次：400，`index: null`、`field: "sets"`，不寫入資料。
- 既有單筆 `POST /api/workouts` 與 `POST /api/workouts/{id}/sets` 回歸正常。

驗證：Claude cross-model review 最終無 blocker、integrity true；Claude canonical acceptance
verifier 逐條判定 **5/5 pass**、integrity true。`uv run pytest` **344 passed**、`uv run ruff
check .` clean、cp950 encoding smoke **63/63**。
