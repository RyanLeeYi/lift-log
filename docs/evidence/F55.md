# F55 — 體重頁「＋ 記錄」按鈕移到畫面下方

2026-07-26：實作者 E2E `verify_f55_own.py` **10/10 PASS**（DOM 順序實測 topbar → body-card → body-card.body-list → btn-primary(＋記錄) → btn-ghost(←回首頁)；613/667/700/844 填滿模式遞減正確、556/500/600 退回模式，全部 overflow=0／over_btn=0）；pytest 189、ruff clean；回歸 f52/f53/f54 全綠。
acceptance-verifier 獨立驗收 ①–⑤ 全 PASS（含 flash 出現時的順序、612/613 門檻邊界、F54/F53 行為回歸；它另用 28 筆資料才讓清單可捲——14 筆在新版面下不會溢出）。
**未派 code reviewer**（明示決定）：diff 是單一元素位置搬移＋版號，風險面只有版面門檻與按鈕順序，兩者已在 7 種 viewport 量測；F54 的 11 條全套回歸亦綠。若日後要補跨 context review，範圍就是這個 commit。
verifier 額外發現（非 fail）：成功送出後 `.screen.body` 節點被重建（整頁重繪），但 `.bm-rows` 的 scrollTop 與 toggle 狀態仍正確保留——那正是 F53 的 captureBodyScroll／rAF 還原與 body.metric 模組狀態在做的事。
