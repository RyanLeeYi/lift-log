# F86 動作表現改版（PR 卡 ＋ 每次最佳組長條圖）

## 狀態
- 畫面實作：2026-07-30 commit `f35bf48`；⑤ 的圖表型別由 F134 取代為可點折線圖（見條目 `superseded_by`）
- ⑩ 隔離區腳本翻新：2026-08-19 commit `553a27e`（executor worker，worktree），verify_f59／f60 改用 `verify_f67` 共用 helper
- 獨立驗收：2026-08-19 `acceptance-verifier`（fresh context，同模型），①–⑩ 全 pass，最高 severity 無

## 完成定義輸出（驗收者實跑）
| 指令 | 結果 |
|---|---|
| `uv run ruff check .` | All checks passed |
| `uv run pytest tests/test_exercises.py` | 10 passed |
| `verify_f86.py` | 38/38 |
| `verify_f59.py` | 8/8 |
| `verify_f60.py` | 8/8 |
| `verify_f134.py`（回歸） | 60/60 |

截圖：`F86_exercise_detail_390x844.png`（Playwright 390x844）

## 驗收備註
- ⑩ 的「`.home-start` → `start_from_home()`」在 F94 通掃時已由 `wait_home()` 取代，本次無可替換點，未強插未使用的呼叫；判定為「目的不變、手段可換」的合理落實
- ⑧ F86 未新增 `:root` token（全數重用既有），無未引用風險
