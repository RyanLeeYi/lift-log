# F133 — sets 組號唯一約束當最後防線（撞了重試，取代行程內鎖）

實作於 worktree 分支後 rebase 到 main、ff 併入（98dbaac）。dev DB 備份：lift-log-backups/liftlog-dev-20260807-pre-f133-migration.db。
前置（migration 先驗資料、撞到就中止）：驗收者以 pre-F133 schema 自建表並手塞一對重複未軟刪 (workout_id=1, exercise_id=1, set_number=3)
   → migrate_schema() 拋 RuntimeError「F133 migration 中止：sets 有重複組號未清理」，且事後 PRAGMA index_list(sets) 確認新索引未被建立（無半殘留）。
① partial unique index 兩半都驗到：真 dev DB 複本上對既有 active 組合插重複 → UNIQUE constraint failed: sets.workout_id, sets.exercise_id, sets.set_number；
   挑只有軟刪列佔用的 (workout_id=47, exercise_id=2, set_number=5) 插入新 active 列 → 成功（軟刪後重用不被擋）。
   臨時拔掉 models.py 的 sqlite_where → test_soft_deleted_set_number_can_be_reused_by_another_client_uuid 由 pass 轉 fail，還原後乾淨（測試真的釘住該條件）。
② 重試：_SET_NUMBER_MAX_RETRIES = 5（常數）。monkeypatch _next_set_number 永遠回傳已佔用的號逼滿 5 次 → 拋 ConflictError（409），
   訊息含 workout_id／exercise_id 與「請重新嘗試」，且該 client_uuid 沒有幽靈列寫入（rows written: 0）。
③ client 帶號撞既有號：TestClient 打真實端點，兩個不同 client_uuid 都帶 set_number=1 → 第二次回 409；
   GET /api/workouts/{id} 覆核 set_number 清單仍只有 [1]，未靜默多寫一列。
④ 移除重試會 fail：把 _SET_NUMBER_MAX_RETRIES 由 5 臨時改成 1 → test_concurrent_log_set_never_reuses_a_set_number 與
   test_retry_recovers_when_assigned_number_is_already_taken 兩條都轉 fail（2 failed, 1 passed），改回後乾淨。
   新增的決定性測試用 monkeypatch 逼撞號，不靠 thread 排程運氣（原本純併發測試可能整輪不撞號而永遠綠——review 抓到的 P2）。
⑤ 不得回歸：286 passed，涵蓋 TestServerAssignedSetNumber（F32 跳號／依動作／依場次）、TestUpdateSet（F16 PATCH 不動 set_number）、
   TestIdempotencyEdges（F131 ④ client_uuid 冪等）、test_omitted_set_number_*（F131 ③ server 配號語意），這些檔案不在本次 diff 內。
⑥ 完成定義：uv run pytest 286 passed、uv run ruff check . All checks passed（rebase 到 main 後重跑）；
   migration 在真 liftlog-dev.db 實跑（主 session，2026-08-07）：
   前 PRAGMA index_list(sets)=ix_sets_exercise_id, ix_sets_workout_id, ix_sets_client_uuid；
   後多出 ix_sets_workout_exercise_set_number_active，DDL＝
   CREATE UNIQUE INDEX ix_sets_workout_exercise_set_number_active ON sets (workout_id, exercise_id, set_number) WHERE deleted_at IS NULL；
   重跑冪等不炸；sets 總筆數 465 前後未變（備份與現況皆 465，驗收者以 SHA-256 獨立確認原檔未被驗收動作改動）。
review findings（headless Claude reviewer，2026-08-07）三條 P2 全修於 98dbaac：IntegrityError 錯誤分類過寬＋from None 抹真因、
   race 測試非決定性（違反 ④ 字面）、partial 條件無測試釘住。一條 P3（migrations.py 沒先檢查 deleted_at 欄位存在）由主 session REJECT——
   reviewer 自己標明為推測、無證據存在那種舊 DB，且 deleted_at 屬原始 schema。
⚠ prod DB 的 index 尚未套用：migrate_schema() 掛在 app/main.py:50 的啟動流程，prod 下次重啟會自動套用（自帶重複資料中止防呆）。
   驗收者判定此為「還沒重啟 prod」的操作待辦，非功能缺口，不擋 passing。prod 舊撞號已於 2026-08-06 清理完畢。
⚠ review 與驗收皆為 Claude（跨 session、跨 context，非跨模型）——Codex 額度用盡的退路。
DEFER：tests/test_api_workouts.py 的 test_omitted_set_number_skips_numbers_of_deleted_sets docstring 還寫著
   「後端沒有組號唯一約束，撞號是靜默的」，F133 之後這句已不成立（過時註解，不在本次 diff 範圍，行為未受影響）。

