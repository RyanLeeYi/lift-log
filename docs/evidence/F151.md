# F151 — 寫入冪等鍵

驗收日：2026-08-13。

`sets` 新增 nullable `idem_key` = `sha256(date|exercise_id|set_number)`，配 partial unique
index（`idem_key IS NOT NULL AND deleted_at IS NULL`）。鍵存於 server，重送不依賴 client 狀態。

- 首次寫入回 `created=2, skipped=0`；重送同一批回 `created=0, skipped=2`，`workouts`／`sets`
  row count 不變（**整批命中時連 `Workout` 列都不新建**）。
- 跨 session／跨裝置：重啟 server 後以無 `client_uuid` 與換一組 `client_uuid` 重送，皆
  `0/2` 且 DB 不變。
- 部分重送：新組併入既有鍵所屬的 workout，不另開一場（避免同日出現只含新組的孤兒
  workout）；`template`／`note` 不覆寫既有 workout。
- F143 層（同 `client_uuid` mutation 冪等）不受影響，兩層同時成立且各有測試。
- non-goals 守住：同日同動作不同 set index 的相同重量次數視為兩組（不做近似去重）；
  既有列不回填，`idem_key` 維持 `NULL`（migration 後實測仍為 NULL）。

`DOMAIN_SCHEMA_VERSION` 2 → 3；既有 DB 由 `app/migrations.py` 冪等補欄位與索引。

驗證：Codex 跨模型 fresh-context 驗收 **6/6 pass**，驗收前後 `git status` 逐字相同
（報告：`codex-verify-F151.md`）。`uv run pytest` **355 passed**、`uv run ruff check .`
clean、cp950 encoding smoke **63/63**。後端 only，未動 `app/static/`，不出 APK。

## 已知上限（規格層，非實作缺陷）

鍵是 date-scoped：**同一天真的分兩次練同一個動作**，第二次的第 1 組會與第一次的第 1 組
撞鍵而被判為重送。凍結 acceptance 明定以 `date+exercise+set_index` 為鍵，實作照辦；
Codex 驗收者獨立提出同一點作為規格回饋。F152 的 dry-run 摘要是這個誤判的煞車。
