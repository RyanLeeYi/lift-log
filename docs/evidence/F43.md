# F43 — 日曆明細動作捲軸＋補記改懸浮 modal

commit ec625bd（含 codex-review 兩修）。E2E scratchpad/verify_f43.py 9/9：① >3 動作出 .cal-ex-scroll 溢出可捲、≤3 不套容器、噸位/狀態/入口不被捲入；② 入口開 .modal-overlay（非內嵌）、點遮罩空白關；③ modal 內搜尋選動作+steppers+累度軸+自體重預設負重 0；④ 記這組自動關 modal+日曆即時刷新+set_number 接續+未來日無入口+不動 state.workoutId；加驗 P2-1 捲動後展開 scrollTop 保留（before=18/after=18）。codex-review 兩條 P2 均修：P2-1 捲軸位置流失（cal.exScrollTop+rAF 還原）、P2-2 modal 送出中競態（closeAddModal if(addSubmitting) return，同 addSet 防重入 pattern）。完成定義 pytest 189 passed+ruff clean（fix 後親跑）。跨-context 驗收：acceptance-verifier 執行中明訊『All acceptance items verified』（已逐條驗完 acceptance，於最後 ruff/pytest 收尾階段被使用者要求停止以節省時間；22+ 版腳本卡在點日曆格 selector 摩擦，非產品問題）。sw.js/state.js v43→v44。
