# F98 — 編輯課表動作卡收合／展開（拖曳時全部收起）

（2026-07-31 v112）① 卡片兩態：收合＝名稱＋「N 組 · 休息 Xs」摘要＋刪除鈕；展開＝原本的完整內容。② 預設全部收合。③ 點卡片切換、互斥（tpl.expandedId 存 exercise_id 不存索引——拖曳排序會讓索引失效）。④ 拖曳中所有卡片一律收起，放開後恢復。⑨ expandedId 刻意不進 tpl.editing，所以 templateSnapshot 看不見它，收放不會被誤判成未儲存變更。
**關鍵實作決定**：收放只切 CSS class、不重建 DOM。④ 要在拖曳中收起全部卡片，那時若重繪整棵清單，drag-sort 手上的節點參照會全部失效、拖到一半整個斷掉。
⑤ 三種手勢分工：drag-sort 新增 swallowNextClick()，在 capture 階段吃掉拖曳結束後瀏覽器補送的那一個 click（320ms 後自動解除，觸控放開不一定有 click，留著會吃掉下一次真點擊）。
verify_f98.py 30/30，**含觸控節**（⑩ 明訂；CDP Input.dispatchTouchEvent）：點一下＝收放、按下就滑＝捲清單且不得順手收放、長按＝拖曳且全部收起、放開後恢復展開狀態、拖完那一下 click 不觸發收放。反面：按刪除鈕不得被收放接管、也不得順手收放別張卡。
回歸：verify_f97 36/36、verify_f88 31/31、verify_f101 16/16、verify_f102 18/18、verify_f99 16/16；ruff clean；pytest 274 passed。
**版面異動需知會**：刪除鈕從控制列移到標題列——收合後標題列是唯一還看得到的一列，刪除鈕留在下面會跟著被收起來。F88 ③「組數靠左、刪除靠右」那半句因此由本條取代；verify_f88 的間距斷言改量兩顆鈕的實際最短距離（不再是單軸差），誤觸防線的本意不變。
