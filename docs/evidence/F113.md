# F113 — 組列的編輯改成懸浮視窗（取代行內展開）

（2026-08-01，v122。verify_f113.py 15/15 ＋ 真機 SM-N9750 dev v122。）

實作：`doneRow` 不再依 `editDraft` 就地展開；改成 `editSetModal()`，沿用既有的
`.modal-overlay`／`.modal` 慣例（點遮罩＝取消）。`editDraft` 仍是唯一的草稿來源，
所以「一次只開一個」是天然結果而不是另一段防呆。行內展開的 CSS（`.done-row.editing`）整組移除。

⑤ 連帶把 `.done-list.scrollable` 的 `&& !editDraft` 拿掉——那是行內表單時代的權宜
（表單要完整可見所以取消限高），改成視窗之後理由消失，而清單高度跳動與 F99／F111
剛整理好的規則相衝。

verify_f113 15/15，重點在 ③：**儲存之後組列是新值，而且重整後仍是新值**。
編輯是唯一會回頭改動已記資料的入口，換一個殼很容易把 PATCH 那條路弄丟，
而畫面上看起來一切正常（視窗開了、按了儲存、視窗關了），只有資料是舊的。
期望值從實際值推算，不寫死——種子資料改了不該讓斷言變成假的（第一版就踩到這個）。

## ⚠ 順手抓到一個既有違規（比本條本身更重要）

`@media (max-height: 840px)` 把步進器的內距與字級收小之後，計時頁 KG／REPS 的 ±
在 **384×727（Ryan 的實機）量到 39px 高**——低於 F74／F77 訂的 44px。
390×844 量到 49px，所以這個違規從那條 media query 存在以來一直沒被抓到。
那是每一組都要按、常常還是濕手在按的按鈕。

已補 `min-height: 44px`，並把 384×727 這個尺寸**永久釘進 verify_f102**（19/19）。

**代價要講清楚**：這 +5px 讓 F109 的 360×640 休息態溢出從 9px 變成 **14px**。
這是刻意的取捨——F109 的條文本身就寫「不得靠壓觸控目標解決」，F74／F77 也是凍結的。
Ryan 的實機 384×727 是 0px，實際不受影響。F109 的 evidence 已同步更新。

回歸：verify_f99 19/19（⑤ 依本條翻面）、verify_f102 19/19、verify_f112 20/20、
verify_f84 48/50（僅剩 F109）。ruff clean。
