# F25 — 自訂動作入口進課表加動作視窗

2026-07-19 完成。抽出共用元件 app/static/js/custom-exercise.js（自我包含：錯誤就地顯示、不動父層重繪），picker（app.js pickerCustomModal）與課表編輯（templates.js templateCustomModal）共用；課表「加動作」視窗內加「＋自訂動作」入口，開建立視窗疊在上層，建立成功重載可選清單並預選新動作可直接「確定加入」。picker 入口保留（兩處都有）。sw.js SHELL 加 /js/custom-exercise.js、CACHE_NAME v19→v20、APP_VERSION 同步 v20。E2E scratchpad verify_f25.py R1–R5 全 PASS（入口+疊層、建立後預選+確認鈕啟用、加入課表、後端建立、picker 入口仍在）；F10 picker 回歸 ALL PASS、F24 版本號回歸 v20；全套 161 passed、ruff clean。codex-review 完成：1 條 P2（templateCustomModal 刷新失敗未 fallback，同 picker 舊 P2）已修（loadPickable 失敗改用建立回傳值補進清單）；無 P0/P1。修後 F25 E2E 仍 ALL PASS、ruff clean。sw.js v20→v21、APP_VERSION v21。
