# F87 體重體脂改版（大數字主卡 ＋ 24 根長條圖，保留時間窗）

## 狀態
- 畫面實作：2026-07-30 起，後續 F119（選 B，五顆藥丸取代自選區間）／F120 疊加
- ⑭ 隔離區腳本翻新：2026-08-19 commit `f12e058`（executor worker，worktree），verify_f53–f58 改用 `verify_f67` 共用 helper
- 獨立驗收：2026-08-19 `acceptance-verifier`（fresh context，同模型），①–⑮ 全 pass，最高 severity 無

## 完成定義輸出（驗收者實跑）
| 指令 | 結果 |
|---|---|
| `./init.sh` | init OK（smoke 152 passed） |
| `uv run ruff check .` | All checks passed |
| `uv run pytest tests/test_body_metrics.py` | 18 passed |
| `verify_f87.py` | 38/38 |
| `verify_f53`–`f58` | 15/15、12/12、11/11、18/18、6/6、10/10 |
| `verify_f59.py`（range.js 共用回歸） | ALL PASS |

截圖：`F87_body_weight_390x844.png`、`F87_body_fat_390x844.png`

## 驗收備註
- ⑪「不得把 min-height 設 0」與實作 `.body-list { flex:none; min-height:0 }` 字面看似矛盾；`app.css` 唯一來源註解明示原意是「不得單獨用 min-height:0 取代 flex:none」，判 pass。下次凍結否定句時加「單獨」等修飾詞
- ⑭ 六支本來就沒有 `.home-start`（走底部導覽直接進 /body），未強插 `start_from_home()`
