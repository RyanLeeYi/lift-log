# F164 evidence：/api/chat 與 LLM key 來源

2026-08-22 實作（dispatch session）。**拍板紀錄**：agent-brief 收件匣回覆「F164 採 (a) server 代打」
——自填 key 由 client 每次請求以 `X-LLM-API-Key` 夾帶，server 代打上游，key 不落地、不進 log。

## 做了什麼

- `app/api/chat.py`（新檔）：`POST /api/chat/`。auth 走既有 `require_domain_auth`；
  legacy scope 一律 403（①）。
- tool 呼叫全部經 F163 的 `run_tool`，綁 `request.state.domain_scope`（＝呼叫者 user id）；
  請求 body 沒有任何身分或 DB 欄位可指定（②）。
- 兩段式寫入（③）：LLM 回 `tool_use` 且是寫入類 tool 時，只跑 `dry_run=True`，
  回 `pending_write` ＋ F152 摘要就結束；真正寫入只發生在 client 帶回 `pending_write`
  的第二次請求，**那條路徑完全不呼叫 LLM**（確認的是使用者，不是模型），
  且只接受寫入類 tool。
  `pending_write` 由 server 以 HMAC 簽章（金鑰沿用 web session 的 CSRF 金鑰），
  確認時逐一比對 `(tool, arguments)`——**這是 2026-08-22 獨立驗收開的 P2**：
  原本只檢查 tool 名在不在寫入清單裡，於是「沒跑過第一段就直接送一份 pending_write」
  也寫得進去，等於整個 dry-run 確認流程可跳過。簽章是無狀態解法，不必為一次確認建表。
- key 來源（④⑤）：header 自填優先，其次 `.env` 的 `LIFTLOG_LLM_API_KEY`；
  兩者皆無回 400 `{"error": "llm_key_missing"}`。
- 上游用既有 `httpx`（⑥），無新依賴；model／base URL／timeout 可由 `.env` 設定，
  預設 Anthropic Messages API。401/403→`llm_unauthorized`(502)、429→`llm_rate_limited`(429)、
  逾時→`llm_timeout`(504)；回應只帶錯誤碼，不含 key 也不含上游 body。
- 單輪（⑦）：不存歷史、不建資料表；每次請求的 messages 只有這一句。

## 驗證

`uv run pytest tests/test_chat.py -q` → 14 passed（上游一律 `httpx.MockTransport` stub，不打外網）：

| 測試 | 條文 |
|---|---|
| `test_chat_rejects_legacy_token` | ① |
| `test_read_tool_runs_as_caller_and_cannot_reach_other_user` | ② |
| `test_write_is_two_phase_dry_run_then_confirm` | ③（未確認前零新增列、零 change log；確認後進 change log；確認不再打 LLM） |
| `test_confirm_without_prior_dry_run_is_rejected` | ③（驗收 P2 回歸：沒跑第一段就送 pending_write → 400 `pending_write_invalid`，零寫入） |
| `test_confirm_rejects_tampered_arguments` | ③（拿到合法簽章後改參數也不行；原封帶回才寫得進） |
| `test_user_supplied_key_wins_and_never_comes_back` | ④（含掃描 tmp 內所有 .db 位元組確認 key 沒落地） |
| `test_server_key_used_when_user_has_none` | ④ |
| `test_chat_without_any_key_returns_llm_key_missing` | ⑤ |
| `test_upstream_errors_become_readable_codes`（3 條參數化） | ⑥ |
| `test_single_turn_sends_no_conversation_history` | ⑦ |
| `test_confirm_rejects_non_write_tool`、`test_chat_tools_are_exactly_the_mcp_tools` | 邊界加固 |

`uv run pytest -q` 全綠（486 條）、`uv run ruff check .` All checks passed。
⑨：F164 的程式碼面（`app/api/chat.py`、`app/config.py`、`app/main.py`、`.env.example`、
`tests/test_chat.py`）未動 `app/static/`，本條不出 APK。
**更正**（2026-08-22 獨立驗收 P2）：標記為 F164 的 commit `247eada` 其實夾帶了一個
`app/static/js/chat.js`——那是 F165 的檔案，被本場 session 的 commit 順序掃進去的，
不是 F164 的交付物。F165 的 APK 交付見 `docs/evidence/F165.md`。
