# F107 — 「可能延遲」只在真的會延遲時才警告（收緊觸發條件）

（2026-07-31，v114。verify_f107.py 13/13 ＋ 回歸。）

判斷依據改成觀測結果：`noteForegroundTakeover(taken)` 在 `scheduleRestNotify()` 裡記下
前景服務接沒接得了手（`liftlog.fgFallbackSeen`，持久化——它描述的是這台裝置的狀況，
不是這次 session）；`nativeExactAlarmOff()` 改成三個條件相乘：通知允許 ＋ 精確鬧鐘被關
＋ 有接手失敗的紀錄。接手成功一次就清掉紀錄。

verify_f107 13/13 分兩層：
  第 1 層 三種組合的渲染結果（權限開有紀錄／權限關無紀錄／權限關有紀錄）
  第 2 層 **紀錄怎麼來的**——真的跑一輪休息，假 plugin 的 start 拋錯＝前景服務啟不動，
         驗紀錄有沒有跟著寫進去／清掉。這層不能省：只驗第 1 層的話，
         「紀錄永遠寫不進去」的實作會全綠，而那等於把 F62 ③ 的出路整條刪掉。

寫測試時踩到一次假綠：假 plugin 少了 `available()`，`startForegroundRest()` 在問它那一步
就 catch 掉 → 看起來像「接不了手」但其實連 start 都沒呼叫到。與 F95 的
「假物件缺了某個 plugin」同族，靠「有沒有真的去問」那條前提斷言抓到。

回歸：verify_f62 35/35（③ 的斷言依本條更新，並補一條反面）、verify_f63 21/21、
verify_f65 8/8、verify_f95 9/9、verify_f106 32/32。ruff clean。

⚠ 未在真機驗證：要湊出「精確鬧鐘被關 ＋ 前景服務起不來」得同時破壞兩個系統條件，
後者在 Ryan 的裝置上正常運作、無法穩定製造。這條的行為全靠 E2E 覆蓋，如實記著。
