# F44 — 日曆補記 modal：取消鍵改為『退一步』（移除多餘的換動作）

verify_f44.py（本機 Playwright E2E）6/6 PASS：①記錄態無「換動作」按鈕 ②取消＝退回選動作態（modal 仍開、搜尋字清空、draft 丟棄→重選為 20kg/8下/輕鬆）③選動作態取消關閉 modal 回日曆、點遮罩空白行為不變 ＋迴歸「記這組仍自動關 modal 並寫入一組」。ruff check 全過；codex review（--uncommitted）無 findings。
